/**
 * FormValidation (https://formvalidation.io)
 * The best validation library for JavaScript
 * (c) 2013 - 2023 Nguyen Huu Phuoc <me@phuoc.ng>
 */

import { type ValidateFunctionInterface, type ValidateOptions, type ValidateResult } from '@form-validation/core';

export interface CallbackOptions extends ValidateOptions {
    callback: ((...arg: unknown[]) => unknown) | string;
}

export function callback(): ValidateFunctionInterface<CallbackOptions, ValidateResult>;
